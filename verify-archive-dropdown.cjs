const fs=require('fs'),vm=require('vm'),assert=require('assert/strict');const h=fs.readFileSync('standalone/Hoopland League Studio.html','utf8');const a=h.indexOf('function attachArchiveDropdown'),b=h.indexOf('function attachImagePreview',a);
const node=(tag,cls,text)=>({tag,text,value:'',children:[],events:{},append(...n){this.children.push(...n)},replaceChildren(...n){this.children=n},setAttribute(){},addEventListener(k,fn){this.events[k]=fn},focus(){}});
const asset={name:'Boston',kind:'Logos',era:'Modern',url:'https://example.com/boston.png',local:'data:image/png;base64,AA',width:64,height:64};const attach=vm.runInNewContext(h.slice(a,b)+';attachArchiveDropdown',{el:node,assets:[asset],Event:class{constructor(type){this.type=type}}});const p=node(),events=[],input={dispatchEvent:e=>events.push(e.type),focus(){}};attach(p,input,'logoURL');const drop=p.children[0];drop.open=true;drop.events.toggle();const layout=drop.children[3],list=layout.children[0],preview=layout.children[1];assert.equal(list.children.length,1);list.children[0].onmouseenter();assert.equal(preview.children[0].src,asset.local);list.children[0].onclick();assert.equal(input.value,asset.url);assert.deepEqual(events,['input','change']);assert.equal(drop.open,false);drop.children[1].value='missing';drop.children[1].oninput();assert.equal(list.children[0].text,'No matching images.');console.log('Passed archive selection, hover preview, search and input/change notifications.');
